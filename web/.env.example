# Neon (production) or local Docker from docker-compose.yml
DATABASE_URL=postgres://kadr:kadr@localhost:5432/kadr

# Session signing (any long random string in production)
AUTH_SECRET=kadr-dev-secret-change-me
